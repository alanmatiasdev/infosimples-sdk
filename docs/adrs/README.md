# Architecture Decision Records

This directory holds ADRs for significant implementation decisions in this project (e.g. choice of
tooling, breaking changes to the public API shape, release process changes).

Currently empty — add one file per decision (e.g. `0001-title-of-decision.md`) as decisions are made.
